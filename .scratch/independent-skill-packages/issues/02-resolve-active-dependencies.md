# 02 — 按当前动作核验已注册依赖并识别合法链接

**What to build:** 用户运行阶段时只会因当前动作缺少必要能力而停止，合法项目/全局链接不会被误报；诊断能区分磁盘存在与当前 Agent 已启用。

**Blocked by:** 01 — 从单一源码生成可独立运行的讨论包。

**Status:** implemented — archived

**Spec:** 独立安装 PRD，D4、D5；AC4；T6、T7。

- [x] 包内只读 preflight 接受可信有效 registry、当前阶段与动作，返回规范身份或明确错误；不创建业务/安装状态。
- [x] 以当前有效注册选择为权威；候选扫描只作诊断，覆盖明确项目根和既有用户根，不覆盖 registry 的选择。
- [x] 合法目录和入口符号链接可解析；同一真实文件去重；悬空、循环、不可读、未激活与歧义分别有稳定诊断。
- [x] 实现 PRD Matt 分阶段/条件分支矩阵；未用能力不拦启动，选择能力后在副作用前检查；不改第三方、不自动安装、不内置替代。
- [x] CLI 无当前 registry 时不能输出 Agent 可调用成功；控制器提供清单的证据来源和子 Agent 自检边界清楚。
- [x] 自动测试真实临时文件与受控 registry 的优先级、权限/链接负例；每个错误包含 required_skill、stage/action 和恢复动作。
- [x] 现有依赖诊断脚本不再要求所有五个内部入口或整套 Matt，不以本机安装清单证明包可用。


## 交付与验证

接受候选 `e8f9a55309766d8436696957c9451e9f62f8940a` 完成此切片；前置切片均已完成。368 项源/包测试、五包 quick validation、仓库校验与两轴审查通过。统一证据及 AC 映射见 [PRD 归档记录](../PRD.md#归档记录)。
