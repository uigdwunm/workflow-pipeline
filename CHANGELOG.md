# 更新日志

## v1.1.0 — 独立安装的工作流 Skill

### 本次更新

- 五个 Skill 均携带本阶段需要的内部文档与脚本，可以分别安装；开发时仍维护一份共享源码。
- 使用源模板和显式资源清单生成安装包，检查传递依赖、链接、导入、内容摘要与生成漂移。
- 从当前宿主有效登记解析 Skill，合法符号链接按真实入口去重；磁盘诊断不再等同于可调用能力。
- Matt Skill 保持外部依赖，只检查当前动作需要的能力；缺失时提示名称与恢复位置，不自动安装或替代。
- 连续执行在开始及交接前检查后续阶段，保留已完成成果；运行固定原包身份，恢复与派发时复核当前登记。
- Stage 3 继续负责实现、测试、审查与保留候选，Stage 4 负责最终合并、归档和清理。

### 升级与兼容

- 用户安装路径保持 `skills/<name>`。每包版本统一为 `1.1.0`；跨阶段兼容依据协议兼容键完全一致，不以发布版本相同替代校验。
- 开发源码迁至 `src/stages` 与 `src/shared`；修改源后运行 `python3 scripts/build_skills.py`，不要手改生成包。
- 新 runner 记录使用 v2。旧 v1 记录不自动迁移，须保留原 runner 与安装树完成旧运行。
- 将新版安装到新的不可变目录，刷新宿主登记；旧运行结束前保留旧目录，不原地覆盖正在使用的包。
- runner 从控制器的 `registry_input` 文件重读登记证据；注册变化后须刷新该文件，或在 resume 时传入 `--registry-input`。更新证据不会更换原执行包身份。

### 验证边界

独立安装实现通过 368 项自动测试及 Standards / Spec 两轴审查。固定安装器 `skills@1.6.0` 已通过临时项目中的五包发现与逐包安装验证。真实宿主中的单包、组合及附着讨论恢复流程（T13/T14）尚未现场验收；自动测试不替代该结论。本次发布不自动更新用户安装。
