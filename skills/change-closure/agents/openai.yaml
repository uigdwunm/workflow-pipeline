interface:
  display_name: "4归档"
  short_description: "Commit closure documents and safely finish implementation"
  default_prompt: "Use $change-closure to verify the implementation merge, commit any closure-owned document updates in one short-lived worktree, integrate and clean that worktree through the minimal worktree protocol, and report exact local and remote outcomes."
policy:
  allow_implicit_invocation: true
