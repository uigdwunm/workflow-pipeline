interface:
  display_name: "3实现"
  short_description: "Commit implementation only while documents use a separate lease"
  default_prompt: "Use $guided-implementation to supervise one Terra/high task in the existing checkout under an exclusive-checkout-v2 repository lease, coordinate any documentation edit through the shared document lease, keep documentation unstaged for 4归档, commit and merge only implementation artifacts, preserve the routed worker/reviewer and canonical Chinese supervision protocols, and enter 4归档 only after verified merge and lease release."
policy:
  allow_implicit_invocation: true
