interface:
  display_name: "3实现"
  short_description: "Implement in one claimed worktree and publish by exact CAS"
  default_prompt: "Use $guided-implementation to supervise one Terra/high task in a dedicated claimed worktree, keep the committed implementation source protected, bind review and acceptance to one exact candidate, publish through the short target-HEAD CAS, and enter 4归档 only after verified integration while retaining the worktree, branch, source protection and execution claim for checkpoint-owned cleanup."
policy:
  allow_implicit_invocation: true
