interface:
  display_name: "1拷问"
  short_description: "Sharpen a target with coordinated document writes"
  default_prompt: "Use $problem-framing to sharpen this target, refuse protected implementation sources, coordinate exact documentation writes through the shared CAS document lease, publish only stage-owned planning documents through the short checkpoint barrier, and offer 执行后续全部流程 only in the final success footer."
policy:
  allow_implicit_invocation: false
