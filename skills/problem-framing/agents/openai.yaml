interface:
  display_name: "1拷问"
  short_description: "Sharpen a target and commit its planning sources"
  default_prompt: "Use $problem-framing to sharpen this target, write only stage-owned planning documents, commit them before implementation, and offer 执行后续全部流程 only in the final success footer."
policy:
  allow_implicit_invocation: false
