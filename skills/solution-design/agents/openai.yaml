interface:
  display_name: "2方案"
  short_description: "Design and publish stage-owned planning artifacts"
  default_prompt: "Use $solution-design to create one Flow Worktree, launch one context-isolated solution-design subagent there, commit and publish only stage-owned planning artifacts, and retain the same flow for 3实现."
policy:
  allow_implicit_invocation: true
