interface:
  display_name: "2方案"
  short_description: "Design and publish stage-owned planning artifacts"
  default_prompt: "Use $solution-design to launch one context-isolated solution-design subagent, write and commit only stage-owned local Spec, ADR, and Ticket artifacts, publish only when authorized, and hand the result to 3实现."
policy:
  allow_implicit_invocation: true
